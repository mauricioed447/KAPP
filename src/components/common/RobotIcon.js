import React from 'react';

export const RobotIcon = ({ className = "w-10 h-10" }) => (
  <div className={`${className} bg-gradient-to-br from-blue-400 to-purple-500 rounded-full flex items-center justify-center text-white font-bold text-lg`}>
    🤖
  </div>
);
